{{#if asset.video}}
<video
  data-file="{{asset.file}}"
  data-aspect="{{asset.aspect}}"
  data-selected="false"
  data-playing="false"
  muted
  loop
  data-categories="{{asset.categories}}"
  class="{{common}} {{class}}"
  data-selected-class="{{common}} {{selected-class}}"
  >
  <source src="{{asset.video}}" type="video/mp4"></source>
</video>
{{else}}
<figure
  data-file="{{asset.file}}"
  style="background-image: url({{asset.file}});"
  data-aspect="{{asset.aspect}}"
  data-selected="false"
  data-categories="{{asset.categories}}"
  class="{{common}} {{class}}"
  data-selected-class="{{common}} {{selected-class}}"
  >
  <img
    class="sr-only"
    src="{{asset.file}}"
    alt="{{asset.title}}" />
</figure>
{{/if}}
